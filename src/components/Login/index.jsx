import { LoginContainer, LoginContent, LoginForm } from "StyledComponents";

const Login = () => (
  <LoginContainer>
    <LoginContent>
      <LoginForm></LoginForm>
    </LoginContent>
  </LoginContainer>
);

export default Login;
